# Copy to .env.local (never commit .env.local)

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Real production domain — used for canonical URLs, hreflang, sitemap,
# OpenGraph and schema. Must be the real domain even on preview deploys.
SITE_URL=https://TODO_DOMAIN.tld
